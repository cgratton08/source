/* ********** Global variables ********** */
:root {
	/* maroon h: 0, s: 100%, l: 25%*/
  --primary-color-h: 0;
  --primary-color-s: 100%;
  --primary-color-l: 25%;
	
  --mainFontColor: black;
  --mainTextShadow: 2px 2px 3px #ccc;	
  --mainFontSize: 10.5px;
  --mainColor: hsl(var(--primary-color-h), var(--primary-color-s), var(--primary-color-l));
  --mainActiveColor: green;
  --mainNonActiveColor: firebrick;
  
  /* button */
  --mainButtonTXColor: white;
  --mainButtonBorderColor: white;
  
  --mainButtonBGColor: hsl(var(--primary-color-h), var(--primary-color-s), var(--primary-color-l));
  --mainButtonBGColorDarker: hsl(var(--primary-color-h), var(--primary-color-s), calc(var(--primary-color-l) - 10%));

}

/* ********** template.jsp ********** */
button:focus {outline:0;}

a:focus {
	color: white;
}
/* ********** Login Menu ********** */

.login {
    margin: auto;   
    width: 99%;
    padding: 150px 0;           
}

/* ********** Header Menu ********** */
.header {
    height:40px; 
    width: 100%;    
    color: white;
    line-height: 25px;    
    background-color: var(--mainColor);    
    font-size: var(--mainFontSize);    	    
}

a.headerMenu:link { text-decoration: none;}
.headerMenu.active {
    background-color: white;
    color: var(--mainColor);
}

/* ********** Side Menu ********** */
.menu {			
	float:left;
	width: 255px;

	margin-top: 11px;
	left: 0px;
    right: 0px;
    bottom: 20px;
	font-size: var(--mainFontSize);
	overflow: auto;
	white-space: nowrap;
	
	color: var(--mainFontColor);
    text-shadow: var(--mainTextShadow);
}

.menu button {
	color: var(--mainFontColor);
    text-shadow: var(--mainTextShadow);
}

.menu ul li a:link, .menu a:visited {
	text-decoration: none;
	color: var(--mainColor);	
}

.menu ul li a:hover {
    color: var(--mainColor);    
    font-weight: bold;    
    white-space: nowrap;
}

.menu ul {
    padding-left: 18px;
    margin-top: 5px;
}


/* ********** container ********** */
.container {
	float:left;
	width:86%;
	font-size: var(--mainFontSize);
	
	margin-top: 30px;
    position:absolute;
    top: 40px;        
    right: 0px;
    bottom: 20px;        
    overflow-y: auto;  
    
	color: var(--mainFontColor);
    text-shadow: var(--mainTextShadow);
}

/* ********** blockUI ********** */

.buttonload {
    background-color: var(--mainColor);
    border: none; /* Remove borders */
    color: white; /* White text */
    padding: 12px 24px; /* Some padding */
    font-size: 16px; /* Set a font-size */
}

/* Add a right margin to each icon */
i.fas {
    margin-left: -12px;
    margin-right: 8px;
    font-size: 1.2em;
}

/* ********** TEXT-REFLECT CSS ********** */
.text-reflect {
    position: relative;
}
.text-reflect:before, .text-reflect:after {
    display: block;
    position: absolute;
    bottom: -.81em; /* You should change this value to fit your font */
    left: 0;
    right: 0;
}
.text-reflect:before {
    content: attr(data-text);
    opacity: .3;

    /* This is how the text is flipped vertically */
    -webkit-transform: scaleY(-1);
    -moz-transform: scaleY(-1);
    -o-transform: scaleY(-1);
    -ms-transform: scale(1,-1);    
    transform: scateY(-1);
}
.text-reflect:after {
    /* Fading using CSS gradient */
    /* Don't forget to change the colors to your background color */
    background: -webkit-gradient(linear, left top, left center, from(rgba(255,255,255,0)), to(rgb(255,255,255)));
    background: -moz-linear-gradient(top, rgba(255,255,255,0), rgb(255,255,255));
    background: -ms-linear-gradient(top, rgba(255,255,255,0), rgb(255,255,255) 52%);
    background: linear-gradient(top, rgba(255,255,255,0), rgb(255,255,255));

    /* I left out the `filter` property, because IE doesn't know `:before` and `:after` pseudo-elements anyway */
    content: ' ';
    height: 1em;
}

/* ********** User List Link CSS ********** */
.userName a:link, .userName a:visited {
	text-decoration: none;
	color: var(--mainColor);	
}

.userName a:hover {
    color: var(--mainColor);    
    font-weight: bold;    
    white-space: nowrap;
    text-decoration: none;
}

